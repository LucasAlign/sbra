# SBRA app environment (filled in at "the swap" — see ticket #6)
# Postgres (Replit provides these automatically when you add a Postgres DB)
DATABASE_URL=

# Auth.js (NextAuth)
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
